CMAKE_MINIMUM_REQUIRED(VERSION 3.1.0)

SET(RUNNABLE_EXE_NAME helloWorld)

ADD_EXECUTABLE(${RUNNABLE_EXE_NAME} "")

TARGET_LINK_LIBRARIES(${RUNNABLE_EXE_NAME} rt pthread)

target_sources(${RUNNABLE_EXE_NAME}
    PRIVATE
        HelloWorld/Source.cpp
)